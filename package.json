{
  "name": "vrnya-client",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=web",
    "dev:desktop": "turbo dev --filter=desktop",
    "dev:waitlist": "turbo dev --filter=waitlist",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
